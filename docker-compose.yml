services:
  bot:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/data/bot.db
      - FAISS_INDEX_DIR=/app/data/faiss_db
    volumes:
      - botdata:/data
      - faiss-data:/app/data/faiss_db
    restart: unless-stopped

volumes:
  botdata:
  faiss-data:
